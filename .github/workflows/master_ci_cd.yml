name: Master CI_CD
on: 
  push:
    branches: [ "master"]
  pull_request:
    branches: [ "master"]
jobs:
  master_ci_cd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js 20.x
        uses: actions/setup-node@v3
        with:
          node-version: 20.x

      - name: Install dependencies
        run: |
          npm install  # Update package-lock.json to match package.json
          npm ci       # Install dependencies based on the updated lock file

      - name: Test
        run: |
          npm test -- --no-watch --no-progress --browsers=ChromeHeadlessCI
  
      - name: Build Angular, output in coverage/unit-tests
        run: |
          CI=false npm run build
          
      - name: Show directory
        run: |
          ls -la coverage/unit-tests

      - name: Download and run httpd image, container name serving_base
        run: |
          docker run -d --name serving_base httpd
          
      - name: List running containers
        run: |
          docker ps
          
      - name: Copy Angular build to container
        run: |
          docker cp coverage/unit-tests/. serving_base:/usr/local/apache2/htdocs

      - name: List container files
        run: |
          docker exec serving_base ls -la /usr/local/apache2/htdocs
